global:
  # co ile sekund Prometheus odpytuje aplikację
  scrape_interval: 15s

scrape_configs:
  # nazwa grupy metryk
  - job_name: "fastapi"
    static_configs:
      # adres aplikacji — "app" to nazwa serwisu w docker-compose
      # port 8000 = port aplikacji
      - targets: ["app:8000"]